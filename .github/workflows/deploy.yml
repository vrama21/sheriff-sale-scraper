name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Migrate Tables
      if: github.ref == 'refs/heads/main'
      environment: ${{ vars.main }}
      run: |
        npx prisma migrate dev
        